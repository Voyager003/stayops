spring:
  application:
    name: stayops
  data:
    mongodb:
      uri: mongodb://localhost:27017/stayops-test?replicaSet=rs0
    redis:
      host: localhost
      port: 6379

toss:
  payments:
    secret-key: test_sk_test_key
    base-url: https://api.tosspayments.com/v1/payments
