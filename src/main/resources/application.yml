spring:
  application:
    name: stayops
  profiles:
    active: local
  mongodb:
    uri: mongodb://localhost:27017/stayops?replicaSet=rs0
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

toss:
  payments:
    secret-key: ${TOSS_SECRET_KEY:test_sk_default}
    base-url: ${TOSS_BASE_URL:https://api.tosspayments.com/v1/payments}
