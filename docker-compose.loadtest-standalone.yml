services:
  mongodb:
    command: ["--bind_ip_all"]

  mongo-init:
    entrypoint: ["sh", "-c", "echo 'standalone mode: skip rs.initiate'"]
    restart: "no"

  app:
    environment:
      SPRING_MONGODB_URI: ${SPRING_MONGODB_URI:-mongodb://mongodb:27017/stayops}
