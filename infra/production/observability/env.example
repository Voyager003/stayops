# Production Observability EC2

PROMETHEUS_CONFIG_PATH=./prometheus.yml
GRAFANA_PASSWORD=replace-with-admin-password

# Prometheus targets are defined in prometheus.yml.
# Register these names in the Route 53 private hosted zone or replace them
# with private IP addresses before deployment:
# app-1.stayops.internal, app-2.stayops.internal, redis.stayops.internal,
# mock-ota.stayops.internal, mongo-1.stayops.internal, mongo-2.stayops.internal,
# mongo-3.stayops.internal.
